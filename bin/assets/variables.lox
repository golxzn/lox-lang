var lhv{ "hello" };
const rhv{ " world" };

{ // all cover test
	const lhv{ "Test" };
	var rhv{};
	rhv = " scope";
	println(lhv + rhv);
}

{ // local & global test
	const lhv{ "Inner" };
	println(lhv + rhv); // Expecting "Inner world"
}

{ // arithmetics
	println(100 * 12 + 4 > 5);
}

var same{ 1 };
{
	var same{ same + 1 };
	println(same);
}

println(lhv + rhv);

lhv = "destroy";
println(lhv + rhv);
